<UserControl x:Class="PasswordManager.Desktop.Views.VaultView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:viewmodels="clr-namespace:PasswordManager.Desktop.ViewModels"
             xmlns:converters="clr-namespace:PasswordManager.Desktop.Converters"
             d:DataContext="{d:DesignInstance Type=viewmodels:VaultViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="900">
    
    <UserControl.Resources>
        <!-- Import Converters -->
        <BooleanToVisibilityConverter x:Key="BoolToVisConverter"/>
        <converters:NullToVisibilityConverter x:Key="NullToVisConverter"/>
        <converters:ZeroToVisibilityConverter x:Key="ZeroToVisConverter"/>
        <converters:InverseZeroToVisibilityConverter x:Key="InverseZeroToVisConverter"/>
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
        <converters:FavoriteIconConverter x:Key="FavoriteIconConverter"/>
        
        <!-- Primary Button Style -->
        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Background" Value="#1976D2"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                CornerRadius="5"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#1565C0"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Background" Value="#BDBDBD"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- List Item Style -->
        <Style x:Key="VaultItemStyle" TargetType="ListBoxItem">
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Margin" Value="0,0,0,5"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#F5F5F5"/>
                </Trigger>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#E3F2FD"/>
                    <Setter Property="BorderBrush" Value="#1976D2"/>
                    <Setter Property="BorderThickness" Value="2"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" 
                Background="White"
                Padding="30,20"
                BorderBrush="#E0E0E0"
                BorderThickness="0,0,0,1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0">
                    <TextBlock Text="My Vault" 
                             FontSize="28"
                             FontWeight="Bold"
                             Foreground="#212121"/>
                    <TextBlock Foreground="Gray"
                             FontSize="14"
                             Margin="0,5,0,0">
                        <Run Text="{Binding TotalItemsCount, Mode=OneWay}"/>
                        <Run Text="items total"/>
                    </TextBlock>
                </StackPanel>

                <Button Grid.Column="1"
                        Command="{Binding AddNewItemCommand}"
                        Style="{StaticResource PrimaryButton}"
                        Padding="20,12">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="➕" FontSize="16" Margin="0,0,8,0"/>
                        <TextBlock Text="Add Item" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>
            </Grid>
        </Border>

        <!-- Search and Filters -->
        <Border Grid.Row="1" 
                Background="White"
                Padding="30,15"
                BorderBrush="#E0E0E0"
                BorderThickness="0,0,0,1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Search Box -->
                <Border Grid.Column="0"
                        Background="#F5F5F5"
                        BorderBrush="#E0E0E0"
                        BorderThickness="1"
                        CornerRadius="5"
                        Padding="12,8">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <TextBlock Grid.Column="0" 
                                 Text="🔍" 
                                 FontSize="16"
                                 VerticalAlignment="Center"
                                 Margin="0,0,10,0"/>

                        <TextBox Grid.Column="1"
                               Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                               Background="Transparent"
                               BorderThickness="0"
                               FontSize="14"
                               VerticalAlignment="Center">
                            <TextBox.Style>
                                <Style TargetType="TextBox">
                                    <Setter Property="Foreground" Value="Black"/>
                                    <Style.Triggers>
                                        <Trigger Property="Text" Value="">
                                            <Setter Property="Background">
                                                <Setter.Value>
                                                    <VisualBrush Stretch="None" AlignmentX="Left">
                                                        <VisualBrush.Visual>
                                                            <TextBlock Text="Search vault items..." 
                                                                     Foreground="Gray" 
                                                                     FontSize="14"/>
                                                        </VisualBrush.Visual>
                                                    </VisualBrush>
                                                </Setter.Value>
                                            </Setter>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBox.Style>
                        </TextBox>
                    </Grid>
                </Border>

                <!-- Filter Buttons -->
                <StackPanel Grid.Column="1" 
                          Orientation="Horizontal"
                          Margin="15,0,0,0">
                    
                    <ToggleButton IsChecked="{Binding ShowFavoritesOnly}"
                                Content="⭐ Favorites"
                                Padding="12,8"
                                Margin="0,0,10,0"/>

                    <Button Command="{Binding ClearFiltersCommand}"
                          Content="Clear Filters"
                          Padding="12,8">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Visibility" Value="Collapsed"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding SearchText}" Value="">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding SearchText}" Value="{x:Null}">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Vault Items List -->
        <ScrollViewer Grid.Row="2" 
                     VerticalScrollBarVisibility="Auto"
                     Padding="30,20">
            
            <Grid>
                <!-- Empty State -->
                <Border x:Name="EmptyState"
                        Background="White"
                        CornerRadius="10"
                        Padding="60"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center">
                    <Border.Style>
                        <Style TargetType="Border">
                            <Setter Property="Visibility" Value="Collapsed"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding FilteredItems.Count}" Value="0">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Border.Style>
                    
                    <StackPanel HorizontalAlignment="Center">
                        <TextBlock Text="🔐" 
                                 FontSize="64"
                                 HorizontalAlignment="Center"
                                 Opacity="0.3"/>
                        <TextBlock Text="No items in your vault" 
                                 FontSize="20"
                                 FontWeight="SemiBold"
                                 Foreground="#757575"
                                 HorizontalAlignment="Center"
                                 Margin="0,20,0,10"/>
                        <TextBlock Text="Click 'Add Item' to create your first password" 
                                 FontSize="14"
                                 Foreground="#9E9E9E"
                                 HorizontalAlignment="Center"/>
                        <Button Command="{Binding AddNewItemCommand}"
                              Style="{StaticResource PrimaryButton}"
                              Margin="0,30,0,0"
                              Padding="25,12">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="➕" FontSize="16" Margin="0,0,8,0"/>
                                <TextBlock Text="Add Your First Item"/>
                            </StackPanel>
                        </Button>
                    </StackPanel>
                </Border>

                <!-- Items List -->
                <ListBox x:Name="ItemsList"
                       ItemsSource="{Binding FilteredItems}"
                       SelectedItem="{Binding SelectedItem}"
                       Background="Transparent"
                       BorderThickness="0"
                       ItemContainerStyle="{StaticResource VaultItemStyle}">
                    <ListBox.Style>
                        <Style TargetType="ListBox">
                            <Setter Property="Visibility" Value="Visible"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding FilteredItems.Count}" Value="0">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </ListBox.Style>
                    
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Border Background="White"
                                    Padding="20"
                                    CornerRadius="8"
                                    BorderBrush="#E0E0E0"
                                    BorderThickness="1">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <!-- Icon -->
                                    <Border Grid.Column="0"
                                          Width="50"
                                          Height="50"
                                          CornerRadius="25"
                                          Margin="0,0,15,0">
                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Setter Property="Background" Value="#E3F2FD"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding Type}" Value="CreditCard">
                                                        <Setter Property="Background" Value="#FFF3E0"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Type}" Value="SecureNote">
                                                        <Setter Property="Background" Value="#F3E5F5"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Type}" Value="Identity">
                                                        <Setter Property="Background" Value="#E8F5E9"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Type}" Value="BankAccount">
                                                        <Setter Property="Background" Value="#E0F2F1"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>
                                        <TextBlock Text="{Binding TypeIcon}"
                                                 FontSize="24"
                                                 HorizontalAlignment="Center"
                                                 VerticalAlignment="Center"/>
                                    </Border>

                                    <!-- Item Info -->
                                    <StackPanel Grid.Column="1" 
                                              VerticalAlignment="Center">
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="{Binding PrimaryDisplay}"
                                                     FontSize="16"
                                                     FontWeight="SemiBold"
                                                     Foreground="#212121"/>
                                            <TextBlock FontSize="14"
                                                     Margin="8,0,0,0"
                                                     Text="{Binding IsFavorite, Converter={StaticResource FavoriteIconConverter}}"/>
                                        </StackPanel>
                                        
                                        <TextBlock Text="{Binding SecondaryDisplay}"
                                                 FontSize="13"
                                                 Foreground="#757575"
                                                 Margin="0,3,0,0">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="Visibility" Value="Collapsed"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding SecondaryDisplay}" Value="{x:Null}">
                                                            <Setter Property="Visibility" Value="Collapsed"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding SecondaryDisplay}" Value="">
                                                            <Setter Property="Visibility" Value="Collapsed"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                        
                                        <TextBlock Text="{Binding TertiaryDisplay}"
                                                 FontSize="12"
                                                 Foreground="#9E9E9E"
                                                 Margin="0,2,0,0">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="Visibility" Value="Collapsed"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding TertiaryDisplay}" Value="{x:Null}">
                                                            <Setter Property="Visibility" Value="Collapsed"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding TertiaryDisplay}" Value="">
                                                            <Setter Property="Visibility" Value="Collapsed"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                    </StackPanel>

                                    <!-- Actions -->
                                    <StackPanel Grid.Column="2"
                                              Orientation="Horizontal"
                                              VerticalAlignment="Center">
                                        
                                        <Button Command="{Binding CopyPasswordCommand}"
                                              ToolTip="Copy Password"
                                              Padding="10"
                                              Margin="5,0"
                                              Background="Transparent"
                                              BorderThickness="0"
                                              Cursor="Hand">
                                            <TextBlock Text="📋" FontSize="16"/>
                                        </Button>

                                        <Button Command="{Binding ToggleFavoriteCommand}"
                                              ToolTip="Toggle Favorite"
                                              Padding="10"
                                              Margin="5,0"
                                              Background="Transparent"
                                              BorderThickness="0"
                                              Cursor="Hand">
                                            <TextBlock Text="{Binding IsFavorite, Converter={StaticResource FavoriteIconConverter}}" 
                                                     FontSize="16"/>
                                        </Button>

                                        <Button Command="{Binding EditCommand}"
                                              ToolTip="Edit"
                                              Padding="10"
                                              Margin="5,0"
                                              Background="Transparent"
                                              BorderThickness="0"
                                              Cursor="Hand">
                                            <TextBlock Text="✏️" FontSize="16"/>
                                        </Button>

                                        <Button Command="{Binding DeleteCommand}"
                                              ToolTip="Delete"
                                              Padding="10"
                                              Margin="5,0"
                                              Background="Transparent"
                                              BorderThickness="0"
                                              Cursor="Hand"
                                              Foreground="#D32F2F">
                                            <TextBlock Text="🗑️" FontSize="16"/>
                                        </Button>
                                    </StackPanel>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </Grid>
        </ScrollViewer>
    </Grid>
</UserControl>