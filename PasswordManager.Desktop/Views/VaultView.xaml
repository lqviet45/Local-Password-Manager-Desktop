<UserControl x:Class="PasswordManager.Desktop.Views.VaultView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:viewmodels="clr-namespace:PasswordManager.Desktop.ViewModels"
             d:DataContext="{d:DesignInstance Type=viewmodels:VaultViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="900">
    
    <UserControl.Resources>
        <!-- List Item Style -->
        <Style x:Key="VaultItemStyle" TargetType="ListBoxItem">
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Margin" Value="0,0,0,5"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#F5F5F5"/>
                </Trigger>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#E3F2FD"/>
                    <Setter Property="BorderBrush" Value="#1976D2"/>
                    <Setter Property="BorderThickness" Value="2"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" 
                Background="White"
                Padding="30,20"
                BorderBrush="#E0E0E0"
                BorderThickness="0,0,0,1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0">
                    <TextBlock Text="My Vault" 
                             FontSize="28"
                             FontWeight="Bold"
                             Foreground="#212121"/>
                    <TextBlock Foreground="Gray"
                             FontSize="14"
                             Margin="0,5,0,0">
                        <Run Text="{Binding TotalItemsCount, Mode=OneWay}"/>
                        <Run Text="items total"/>
                    </TextBlock>
                </StackPanel>

                <Button Grid.Column="1"
                        Command="{Binding AddNewItemCommand}"
                        Style="{StaticResource PrimaryButton}"
                        Padding="20,12">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="➕" FontSize="16" Margin="0,0,8,0"/>
                        <TextBlock Text="Add Item" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>
            </Grid>
        </Border>

        <!-- Search and Filters -->
        <Border Grid.Row="1" 
                Background="White"
                Padding="30,15"
                BorderBrush="#E0E0E0"
                BorderThickness="0,0,0,1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Search Box -->
                <Border Grid.Column="0"
                        Background="#F5F5F5"
                        BorderBrush="#E0E0E0"
                        BorderThickness="1"
                        CornerRadius="5"
                        Padding="12,8">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <TextBlock Grid.Column="0" 
                                 Text="🔍" 
                                 FontSize="16"
                                 VerticalAlignment="Center"
                                 Margin="0,0,10,0"/>

                        <TextBox Grid.Column="1"
                               Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                               Background="Transparent"
                               BorderThickness="0"
                               FontSize="14"
                               VerticalAlignment="Center">
                            <TextBox.Style>
                                <Style TargetType="TextBox">
                                    <Style.Triggers>
                                        <Trigger Property="Text" Value="">
                                            <Setter Property="Tag" Value="Search vault items..."/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBox.Style>
                        </TextBox>
                    </Grid>
                </Border>

                <!-- Filter Buttons -->
                <StackPanel Grid.Column="1" 
                          Orientation="Horizontal"
                          Margin="15,0,0,0">
                    
                    <ToggleButton IsChecked="{Binding ShowFavoritesOnly}"
                                Content="⭐ Favorites"
                                Padding="12,8"
                                Margin="0,0,10,0"/>

                    <Button Command="{Binding ClearFiltersCommand}"
                          Content="Clear Filters"
                          Padding="12,8"
                          Visibility="{Binding SearchText, Converter={StaticResource NullToVisibilityConverter}}"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Vault Items List -->
        <ScrollViewer Grid.Row="2" 
                     VerticalScrollBarVisibility="Auto"
                     Padding="30,20">
            
            <Grid>
                <!-- Empty State -->
                <Border Visibility="{Binding FilteredItems.Count, Converter={StaticResource ZeroToVisibilityConverter}}"
                        Background="White"
                        CornerRadius="10"
                        Padding="60"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center">
                    <StackPanel HorizontalAlignment="Center">
                        <TextBlock Text="🔐" 
                                 FontSize="64"
                                 HorizontalAlignment="Center"
                                 Opacity="0.3"/>
                        <TextBlock Text="No items in your vault" 
                                 FontSize="20"
                                 FontWeight="SemiBold"
                                 Foreground="#757575"
                                 HorizontalAlignment="Center"
                                 Margin="0,20,0,10"/>
                        <TextBlock Text="Click 'Add Item' to create your first password" 
                                 FontSize="14"
                                 Foreground="#9E9E9E"
                                 HorizontalAlignment="Center"/>
                        <Button Command="{Binding AddNewItemCommand}"
                              Style="{StaticResource PrimaryButton}"
                              Margin="0,30,0,0"
                              Padding="25,12">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="➕" FontSize="16" Margin="0,0,8,0"/>
                                <TextBlock Text="Add Your First Item"/>
                            </StackPanel>
                        </Button>
                    </StackPanel>
                </Border>

                <!-- Items List -->
                <ListBox ItemsSource="{Binding FilteredItems}"
                       SelectedItem="{Binding SelectedItem}"
                       Background="Transparent"
                       BorderThickness="0"
                       ItemContainerStyle="{StaticResource VaultItemStyle}"
                       Visibility="{Binding FilteredItems.Count, Converter={StaticResource InverseZeroToVisibilityConverter}}">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Border Background="White"
                                    Padding="20"
                                    CornerRadius="8"
                                    BorderBrush="#E0E0E0"
                                    BorderThickness="1">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <!-- Icon -->
                                    <Border Grid.Column="0"
                                          Width="50"
                                          Height="50"
                                          Background="#E3F2FD"
                                          CornerRadius="25"
                                          Margin="0,0,15,0">
                                        <TextBlock Text="{Binding TypeIcon}"
                                                 FontSize="24"
                                                 HorizontalAlignment="Center"
                                                 VerticalAlignment="Center"/>
                                    </Border>

                                    <!-- Item Info -->
                                    <StackPanel Grid.Column="1" 
                                              VerticalAlignment="Center">
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="{Binding Name}"
                                                     FontSize="16"
                                                     FontWeight="SemiBold"
                                                     Foreground="#212121"/>
                                            <TextBlock Text="⭐"
                                                     FontSize="14"
                                                     Margin="8,0,0,0"
                                                     Visibility="{Binding IsFavorite, Converter={StaticResource BoolToVisibilityConverter}}"/>
                                        </StackPanel>
                                        
                                        <TextBlock Text="{Binding Username}"
                                                 FontSize="13"
                                                 Foreground="#757575"
                                                 Margin="0,3,0,0"
                                                 Visibility="{Binding Username, Converter={StaticResource NullToVisibilityConverter}}"/>
                                        
                                        <TextBlock Text="{Binding Url}"
                                                 FontSize="12"
                                                 Foreground="#9E9E9E"
                                                 Margin="0,2,0,0"
                                                 Visibility="{Binding Url, Converter={StaticResource NullToVisibilityConverter}}"/>
                                    </StackPanel>

                                    <!-- Actions -->
                                    <StackPanel Grid.Column="2"
                                              Orientation="Horizontal"
                                              VerticalAlignment="Center">
                                        
                                        <Button Command="{Binding CopyPasswordCommand}"
                                              ToolTip="Copy Password"
                                              Padding="10"
                                              Margin="5,0">
                                            <TextBlock Text="📋" FontSize="16"/>
                                        </Button>

                                        <Button Command="{Binding ToggleFavoriteCommand}"
                                              ToolTip="Toggle Favorite"
                                              Padding="10"
                                              Margin="5,0">
                                            <TextBlock Text="{Binding IsFavorite, Converter={StaticResource FavoriteIconConverter}}" 
                                                     FontSize="16"/>
                                        </Button>

                                        <Button Command="{Binding EditCommand}"
                                              ToolTip="Edit"
                                              Padding="10"
                                              Margin="5,0">
                                            <TextBlock Text="✏️" FontSize="16"/>
                                        </Button>

                                        <Button Command="{Binding DeleteCommand}"
                                              ToolTip="Delete"
                                              Padding="10"
                                              Margin="5,0"
                                              Foreground="#D32F2F">
                                            <TextBlock Text="🗑️" FontSize="16"/>
                                        </Button>
                                    </StackPanel>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </Grid>
        </ScrollViewer>
    </Grid>
</UserControl>
