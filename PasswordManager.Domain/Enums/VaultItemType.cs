namespace PasswordManager.Domain.Enums;

/// <summary>
/// Types of vault items supported
/// </summary>
public enum VaultItemType
{
    Login = 0,
    SecureNote = 1,
    CreditCard = 2,
    Identity = 3,
    BankAccount = 4
}